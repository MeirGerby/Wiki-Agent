{
  "name": "@jarvis/model-catalog-web",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/model-catalog/web/src",
  "projectType": "application",
  "tags": ["type:web", "scope:model-catalog"],
  "// targets": "build/serve/preview inferred by @nx/vite/plugin — run: nx show project @jarvis/model-catalog-web --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.app.json",
        "cwd": "apps/model-catalog/web"
      },
      "cache": true,
      "inputs": ["default", "^production"],
      "dependsOn": ["^build"]
    }
  }
}
