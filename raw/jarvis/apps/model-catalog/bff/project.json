{
  "name": "@jarvis/model-catalog-bff",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/model-catalog/bff/src",
  "projectType": "application",
  "// targets": "build/serve/preview inferred by @nx/vite/plugin — run: nx show project @jarvis/model-catalog-bff",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.app.json",
        "cwd": "apps/model-catalog/bff"
      },
      "cache": true,
      "inputs": ["default", "^production"],
      "dependsOn": ["^build"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "apps/model-catalog/bff"
      },
      "cache": true,
      "inputs": ["default", "^production"],
      "outputs": []
    }
  },
  "tags": ["type:bff", "scope:model-catalog"]
}
