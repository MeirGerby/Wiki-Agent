# Grep Notes
Grep stands for Global Regular Expression Print.
It reads from standard input (stdin) or files, filters rows matching a pattern, and prints them to stdout.
You can use regex like ^ for start of line, or -r for recursive search in directories.
Example: grep -r "error" ./logs
